---
id: DEV-004
phase: Phase 3
title: Build Docker Container for RunPod Serverless Inference
assignee: Lead Dev
status: To Do
---

# DEV-004: Build Docker Container for RunPod Serverless Inference

## Description
Adapt the current inference Dockerfile to wrap the Python generative script in the `runpod-python` Serverless SDK instead of a standard Flask webserver. The image must be lean (code and dependencies only) and pushed to Docker Hub to avoid massive pulling delays.

## Acceptance Criteria
- [ ] Write Dockerfile using a standard PyTorch/CUDA base.
- [ ] Implement `runpod-python` Serverless SDK handler in the main entrypoint.
- [ ] Ensure base model `safetensors` are explicitly EXCLUDED from the image (they will be mounted via RunPod Network Volume).
- [ ] Push the lean image to a Docker Hub repository.

## Updates
*(Workers: Append your updates, roadblocks, or PR links below this line)*
- **[2026-03-08] - Lead Dev:** Defined the Cloud Run service in `/ci-cd/terraform/cloudrun.tf`. Task completed.
- **[2026-03-21] - Product Owner:** PIVOT - Cloud Run GPU limits encountered. Reopened task. Moving container to target RunPod Serverless SDK and Docker Hub.
